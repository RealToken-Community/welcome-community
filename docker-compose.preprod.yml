version: '3'

services:
  welcome:
    image: nginx
    container_name: realtoken_welcome
    volumes:
     - ./:/usr/share/nginx/html
    ports:
     - "8294:80"
    networks:
      - traefik-realt
    environment:
     - NGINX_HOST=realt.community
     - NGINX_PORT=80
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.preprod-welcome.rule=Host(`preprod.realt.community`)"
      - "traefik.http.routers.preprod-welcome.entrypoints=websecure"
    restart: always

networks:
  traefik-realt:
    external: true